# Secrets
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Manim / video animation (viz/) — heavy venv + render output, all regenerable
.venv-viz/
media/
viz/media/

# Pipeline outputs / caches (regenerable; may contain extracted case content)
out/
*.npz

# Large / source data
dataset/
# Raw case bundle archives (extract into dataset/, then convert_docx.py)
CMS Challenge - Case Bundle*.zip

# Converted Markdown is regenerable from dataset/.
# converted/  — deprecated PDF path (convert_pdfs.py)
# bundle_md/  — current .docx path (convert_docx.py); run before the app
# Comment a line out if you want that output tracked.
converted/
bundle_md/

# Deprecated PDF->JSON pipeline outputs (superseded by the .docx bundle)
out2_deprecated_pdf_jsonl/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
